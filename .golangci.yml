version: "2"

run:
  timeout: 3m
  modules-download-mode: readonly

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - ineffassign

issues:
  max-issues-per-linter: 50
  max-same-issues: 3
